def reversestring(s):
    if len(s) == 0:
        return s
    else:
        return reversestring(s[1:]) + s[0]
originalstring = "Hello, World!"
reversedstring = reverse_string(originalstring)
print("Original String:", originalstring)
print("Reversed String:", reversedstring)
